const Footer = () => (
  <footer className="border-t border-gray-100 bg-white py-4 text-center text-sm text-gray-500">
    © {new Date().getFullYear()} TaskFlow — Built with the MERN Stack
  </footer>
);

export default Footer;